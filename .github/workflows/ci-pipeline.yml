name: CI Pipeline
on:
  workflow_dispatch:
  pull_request:
jobs:
  call-lint:
    uses: $/.github/workflows/ci-lint.yml
    with:
      uv-version: "0.12.1"
  call-test:
    needs: call-lint
    uses: $/.github/workflows/ci-test.yml
    with:
      uv-version: "0.12.1"
